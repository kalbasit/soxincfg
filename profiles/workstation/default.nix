{ ... }:

{
  soxin = {
    hardware = {
      bluetooth.enable = true;
    };

    programs = {
      neovim.enable = true;
    };
  };

  soxincfg = {
    programs = {
      git.enable = true;
    };
  };
}
